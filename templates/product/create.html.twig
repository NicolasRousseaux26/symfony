{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-secondary">Ajouter le produit</botton>
        {{ form_end(form) }}
    </div>
{% endblock %}