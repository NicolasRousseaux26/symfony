{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hello {{ name }}</h1>

    <ul>
        <li>
            <a href="{{ path('hello') }}">Nicolas</a>
        </li>
        <li>
            <a href="{{ path('hello', { name: 'fiorella' }) }}">Fiorella</a>
        </li>
    </ul>
{% endblock %}